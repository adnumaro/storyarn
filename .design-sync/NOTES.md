# design-sync notes — Storyarn

- **2026-09-01, first sync — styles/tokens only, by explicit user choice.** Storyarn is a Vue 3 app (LiveVue/Phoenix), not a React component library: no `dist/`, no Storybook, no `*.stories.*`. Claude Design consumes compiled React components, so a component sync is impossible without reimplementation (forbidden). The user chose the styles-only scope over stopping or Vue-wrapping.
- **Bundle contents** (built by hand into `ds-bundle/`, untracked): `styles.css` (base + verbatim chrome classes from `assets/css/app.css`: `.surface-panel`, `.toolbar-btn`, `.toolbar-input`, `.dock-*`), `tokens/colors.css` (verbatim `:root`/`.dark` HSL tokens + plain-CSS `--color-*` aliases replacing the Tailwind v4 `@theme inline` mapping — aliases declared on both `:root` and `.dark` because var() in custom properties resolves at the declaring element), `fonts/` (Courier Prime ×4 woff2 + `@font-face`), `README.md` (= `.design-sync/conventions.md` + inventory footer, stitched manually — off-script, no converter run).
- **No `_ds_sync.json` uploaded** — off-script shape with no verification recipe; un-anchored is the honest state. A re-sync re-builds and re-uploads everything (cheap: 8 files).
- **Excluded on purpose:** `assets/css/screenplay.css` layout classes (`.screenplay-page`, `.sp-character`, …) reference legacy DaisyUI-era tokens (`--color-base-200`, `--color-error`) that no longer exist in the app theme — shipping them would ship broken styles. Only the Courier Prime `@font-face` blocks were taken from that file. Also excluded: `language-flags.css`, `player.css` (product-internal surfaces), Tailwind utility output (designs get generic Tailwind; only tokens/chrome are Storyarn-specific).
- **UI font is the system sans stack** (Tailwind v4 default `--font-sans`); Courier Prime is screenplay/dialogue-only.
- **Environment gotcha:** mid-run, `ds-bundle/` was deleted out from under the session and unrelated `lib/storyarn/projects/assets/execution/*` modifications appeared in the tree — another session/process was active in this working tree. Verify `ds-bundle/` still exists immediately before any `write_files`.
