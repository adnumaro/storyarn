/** The title above each block of the decision detail, so the blocks read apart from their content. */
export const sectionHeading =
  "text-[11px] font-semibold tracking-wider text-muted-foreground uppercase";
