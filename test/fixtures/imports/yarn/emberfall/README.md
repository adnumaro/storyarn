# Emberfall Yarn fixture

This Storyarn-owned fixture models a small interactive quest across three Yarn
source files. It deliberately covers project-file discovery, shared variables,
multiple speakers, conditional dialogue, choices, assignments, cross-file
jumps, line IDs, and terminal control flow.

Tests package this directory as a ZIP at runtime so the repository keeps the
human-readable Yarn sources as the source of truth.
